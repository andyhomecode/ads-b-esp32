// secrets.h -- API keys, compiled in, NOT committed.
//
// Copy this file to include/secrets.h (same directory) and fill in your keys.
// include/secrets.h is listed in .gitignore so it never lands on GitHub.
// If include/secrets.h is missing the firmware still builds; features that
// need a key just stay dark.

#pragma once

// MTA BusTime (SIRI). Free key from https://register.developer.obanyc.com/
#define BUSTIME_API_KEY "00000000-0000-0000-0000-000000000000"
